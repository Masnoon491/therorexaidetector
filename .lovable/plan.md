

## Certificate & Editor Refactor: Professional Disclaimers and Color-Coded Audits

### 1. ContentEditor.tsx -- Scanning Guidelines Box

Add a styled info box between the toolbar and the textarea with three disclaimers:
- **Context Rule**: "Always scan the full text. Scanning fragments significantly increases the risk of false positives."
- **Hybrid Alert**: "Hybrid text (Human + AI) or content edited with software may trigger false positive results."
- **Human Judgment**: "Results are probabilistic estimates. Final verification requires human judgment."

Use an `Alert` component with `AlertCircle` icon, styled with the existing border/muted palette.

### 2. generatePdfReport.ts -- Complete Redesign

Rewrite the PDF to match the uploaded reference layout:

**Page 1 -- Content Quality Report:**
- **Header**: Navy bar with "Generated by **Theorex Consulting**" + "WhatsApp: +8801819185751"
- **Hero banner** (Navy background): "Content Quality Report" title + color-coded badge ("Human Written" in Teal or "AI Written" in Red `#E53935`) + "Words: X | Date: DD-MM-YYYY"
- **Score cards** (2x2 grid): Human Written %, AI Detected %, Plagiarism %, and a placeholder (remove Readability/Grade)
- **Executive Summary box**: Auto-generated text summarizing scores
- **Quality Signals Breakdown**: Horizontal bar chart rows for AI Pattern Level, Human Originality
- **Sentence-by-Sentence AI Analysis table**: #, Sentence Content, Label (Human/Mixed/AI color-coded), Confidence bar + percentage

**Color-coding logic:**
- If AI score > 50%: headers/accents use Alert Red (`#E53935`), badge says "AI Written"
- If AI score <= 50%: headers/accents use Teal (`#00B894`), badge says "Human Written"

**Last page footer:**
- Large Navy rounded-rect footer with Teal accent bar on top
- "Generated by **Theorex Consulting**" + "WhatsApp: +8801819185751"
- Disclaimer line below separator

**Remove entirely:** Readability metrics section, Grammar section, Fact Check section from PDF.

**Every page disclaimers (header/footer):**
- Top of each page (small text): "Results are probabilistic estimates. Always apply human judgement."
- Bottom of each page: Three disclaimer lines:
  1. "Results are probabilistic estimates. Always apply human judgement."
  2. "Hybrid Text (AI + Human Generated) or Human Generated and Edited using any software, can also result in false positives."
  3. "Partial context scans may result in false positives; full context is necessary for accurate AI reporting."

### 3. ReportMeta Update

Add `userEmail?: string` to the `ReportMeta` interface. Pass user email from `ResultsPanel.tsx` (sourced from AuthContext).

### 4. Files to Edit

| File | Change |
|------|--------|
| `src/components/ContentEditor.tsx` | Add Scanning Guidelines alert box above textarea |
| `src/utils/generatePdfReport.ts` | Full rewrite matching reference layout, color-coded branding, disclaimers on every page, remove readability/grammar/facts |
| `src/components/ResultsPanel.tsx` | Pass `userEmail` to `generatePdfReport` |

### 5. No Changes To
- `Dashboard.tsx`, `ScanHistoryPanel.tsx`, `Admin.tsx` -- all untouched

